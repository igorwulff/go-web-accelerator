package component

type InputProps struct {
    Type string
    Name string
    Label string
}

templ Input(props InputProps) {
    <label>{props.Label}</label><input type={props.Type} name={props.Name} />
}